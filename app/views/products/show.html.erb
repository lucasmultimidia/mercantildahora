<div class="row">
  <div class="col-lg-12">
    <h1> Detalhes do produto </h1>
      <p id="notice"><%= notice %></p>
      <br><br>

      <div class="card">
          <table class="table">
            <tr>
              <td><b>Nome:</b></td>
              <td><%= @product.name %></td>
            </tr>
            <tr>
              <td><b>Preço:</b></td>
              <td><%= @product.price %></td>
            </tr>
            <tr>
              <td><b>Detalhes:</b></td>
              <td><%= @product.description %></td>
            </tr>
          </table>
      </div>
      <br><br>

    

      <%= link_to 'Voltar', products_path, class:'btn btn-primary' %>
      <%= link_to 'Adicionar Novo Produto', new_product_path(@product), class:'btn btn-primary' %>
      <%= link_to "Estoque", new_movement_product_path(@product), class:'btn btn-primary' %>
  </div>
</div>
